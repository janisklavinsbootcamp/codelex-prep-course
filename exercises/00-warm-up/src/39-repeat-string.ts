export { };

const repeatString = (letter, multiplier) => { return letter.repeat(multiplier) };

console.log(repeatString("a", 4)); // Expected output: 'aaaa'
console.log(repeatString("b", 5)); // Expected output: 'bbbbb'
